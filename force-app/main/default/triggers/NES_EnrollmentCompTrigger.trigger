/**
 * Created by ajith on 23/10/18.
 */

trigger NES_EnrollmentCompTrigger on Enrollment_Component__c (before insert, before update, before delete, after insert, after update, after delete, after undelete) {

   /* NES_EnrollmentCompHandler handler = new NES_EnrollmentCompHandler();
    if(Trigger.isUpdate && Trigger.isBefore){
        handler.handleBeforeUpdate( Trigger.newMap, Trigger.oldMap);
    }
    if(Trigger.isInsert && Trigger.isBefore){
        handler.handleBeforeInsert( Trigger.new);
    }*/
}